import Navigation from './navigation';

function Header(props) {

    return (
        <header role="banner" className="navbar navbar-expand navbar-dark">
            <Navigation />
        </header>
    );
}

export default Header;
